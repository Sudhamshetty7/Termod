pipeline {
    agent any
       stages {
        stage('Checkout') {
            steps {
                'git branch: 'main', url: 'https://github.com/Sudhamshetty7/Termod.git'
            }
        } 
    }
}
